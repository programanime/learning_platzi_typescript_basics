enum PhotoOrientation {
    Landscape=10, 
    Portrait,
    Square,
    Panorama
}
const value:PhotoOrientation =  PhotoOrientation.Landscape

console.log(value)
console.log(PhotoOrientation[value])

enum Country {
    Bolivia = "bol",
    Colombia = "col",
    Mexico = "mex"
}

const bol:Country = Country.Bolivia;

const some:object = {nombre:"daniel"}

enum  MyData {
    Daniel = "nose",
    Other = 1,
    Lastname = 10.0
}

enum MyData {
    Molina = "molina"    
}
